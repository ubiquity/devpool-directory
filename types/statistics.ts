export type Statistics = {
  rewards: {
    notAssigned: number;
    assigned: number;
    completed: number;
    total: number;
  };
  tasks: {
    notAssigned: number;
    assigned: number;
    completed: number;
    total: number;
  };
};
